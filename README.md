# easyJoystick
easyJoystick is a lightweight Arduino library for analog joystick modules. It handles centre calibration, deadzone filtering, axis swapping, percentage output, and 8-direction detection out of the box. The built-in button debouncer makes it reliable for robotics, game controllers, and menu navigation
